%h1 checkout here
%h2you have follwing items.
%ul
  -@cart.items.each do |item|
    %li 
      = item.quantity
      = item.product.name
      = item.total_price 

%p= @cart.total_price

=form_for @order_form, url: orders_path do |f|
  %p